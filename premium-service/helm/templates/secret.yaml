# Apply manually with real values — never commit actual secrets to Git.
# kubectl create secret generic premium-service-secrets \
#   --from-literal=db-user=postgres \
#   --from-literal=db-password=CHANGEME \
#   --from-literal=razorpay-key-id=rzp_test_XXXXX \
#   --from-literal=razorpay-key-secret=XXXXXXXX \
#   --from-literal=razorpay-webhook-secret=XXXXXXXX \
#   --from-literal=razorpay-account-number=XXXXXXXXXX \
#   -n kawaachai
#
# This template is a placeholder showing the expected keys.
apiVersion: v1
kind: Secret
metadata:
  name: premium-service-secrets
  namespace: kawaachai
  annotations:
    # Managed externally — do not overwrite with helm apply
    helm.sh/resource-policy: keep
type: Opaque
# stringData keys must be populated before deploying:
# db-user, db-password, razorpay-key-id, razorpay-key-secret,
# razorpay-webhook-secret, razorpay-account-number